#include <bits/stdc++.h>
using namespace std;
int main()
{
    int w,l;
    while (EOF)
    {
        cin >> l >> w;
        {
            if (w == 0 && l == 0)
                break;
                float m=w/l;

            float ans;
            ans = (w + l) - (m + 0.7500);
            printf("%.4f\n", ans);
        }
    }
}